{
    "isEnableDBValueInsertUpdateMode()" : {
        "description": "是否启用数据库值插入更新模式，默认为不启用，使用值函数模式",
        "params": {
        },
        "return": "boolean",
        "returnDescription": "是否启用数据库值插入更新模式，默认为不启用，使用值函数模式"
    }
}